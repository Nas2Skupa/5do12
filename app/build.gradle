apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias '5do12'
            keyPassword 'nas2skupa'
            storeFile file('/Users/Ranko/AndroidStudioProjects/nas2skupa.jks')
            storePassword 'nas2skupa'
        }
    }
    compileSdkVersion 23
    buildToolsVersion '23.0.2'
    defaultConfig {
        applicationId "com.nas2skupa.do12"
        minSdkVersion 11
        targetSdkVersion 23
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt', 'proguard-project.txt'
            signingConfig signingConfigs.config
        }
    }
    productFlavors {
    }
}

dependencies {
    compile 'io.nlopez.smartlocation:library:3.2.0'
    compile files('libs/slider.jar')
    compile 'com.google.android.gms:play-services:8.4.0'
    compile 'org.jbundle.util.osgi.wrapped:org.jbundle.util.osgi.wrapped.org.apache.http.client:4.1.2'
}
